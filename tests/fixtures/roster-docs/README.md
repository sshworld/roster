# Roster Docs Fixture

This is a project README, not an agent definition. It should be excluded from
dir source scans (R0 regression) — previously misdetected as an "agent" and
produced spurious overlap findings against other README files.
